### 第四步：04. Mail（邮件通知）

测试跑完了，怎么让团队知道？

1. **Jenkins 原生邮件配置**：在 Jenkins 系统设置里，配置 SMTP 服务器（QQ邮箱/163邮箱的企业版或126邮箱）。
    
2. **Allure 邮件集成**：使用 **Email Extension Plugin**，在构建后配置发送邮件。
    
3. **邮件内容模板**：可以设置当测试结果**失败率超过阈值（如0.1%）或构建失败时**，自动发送邮件给测试组和开发组，邮件正文包含：**执行时间、通过率、响应时间图表（Allure 截图）**。