x = "global"          # G （Global）


def outer():
    y = "enclosing"   # E （Enclosing 嵌套）
    
    def inner():
        z = "local"   # L （local 局部）
        print(z)      # 先找 L → 找到 z
        print(y)      # L 没有 → 找 E → 找到 y
        print(x)      # L 没有 → E 没有 → 找 G → 找到 x
        print(len)    # L → E → G 都没有 → 找 B → 找到 len

inner()
outer()

Local 局部
Enclosing 嵌套
Global 全局
Built-in 内置